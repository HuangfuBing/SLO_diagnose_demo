# 权重目录（占位说明）

为需要本地或网络挂载的模型权重预留的目录：

- `spm/`：存放 SPM 相关的 backbone、校准权重与先验矩阵；
- `vlm/`：存放 VLM 本体或 LoRA/adapter 等增量权重；
- `prompt/`：若有模板或 tokenizer 相关文件，可统一放置在此。

仓库未提供真实权重，本文件仅作为 dummy 占位提醒。运行时可通过环境变量指向这些路径，或将实际文件挂载到对应子目录。
