# 配置文件目录（占位说明）

用于存放与部署/训练环境相关的配置文件，例如：

- `spm_backbone.yaml`：SPM 主干的模型定义；
- `calib_settings.yaml`：校准/阈值调整参数；
- `vlm_runtime.json`：VLM 推理时的设备、dtype 等运行参数。

当前仓库未附带真实配置，提交了该占位文件以便在补充实际内容时有明确位置。根据你的环境替换为真实配置或在同名文件中记录路径。若需要区分环境，可在本目录新增 `dev/`、`prod/` 子目录并使用 `.env` 或 `*.yaml` 文件分开管理。
