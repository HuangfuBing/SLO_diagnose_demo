# 数据目录（占位说明）

预留用于本地化存放示例影像或开发/验证数据集的目录，便于在无外部存储依赖下快速运行 Demo。

建议：
- 在 `samples/` 子目录放置少量脱敏测试图像，并在需要时更新 `README` 说明来源与用途；
- 在 `experiments/` 子目录整理实验或回归数据。

当前文件仅作占位，实际使用时请根据隐私与合规要求上传或挂载数据，不要将敏感数据直接提交到仓库。
